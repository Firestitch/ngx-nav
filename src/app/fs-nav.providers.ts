import { InjectionToken } from '@angular/core';


export const FS_NAV_DEFAULT_CONFIG = new InjectionToken<any[]>('fs-nav.default-config');
export const FS_NAV_CONFIG = new InjectionToken<any[]>('fs-nav.config');
